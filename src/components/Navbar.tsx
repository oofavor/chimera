import { styled } from '@nextui-org/react';

export const Navbar = () => {
  return <Wrapper></Wrapper>;
};

const Wrapper = styled('div', {
  height: 56,
  width: '100%',
  backgroundColor: '#aaa',
});
